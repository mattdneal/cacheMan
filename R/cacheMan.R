#' An in-memory cache manager for R
#'
#' cacheMan is a simple in-memory cache manager for R. Functions can be wrapped
#' to allow them to retrieve results from memory rather than recalculating them
#' when their parameters have not changed.
#'
"_PACKAGE"
